spring:
  application:
    name: onboarding
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user